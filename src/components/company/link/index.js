import styled from 'styled-components';
import { Link } from "react-router-dom";

export default styled(Link)`
  color: blue;
  font-family: 'Arial';
  font-size: 18px;
  &:visited {
    color: blue;
  }
`;
